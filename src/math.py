"""
Math utils
"""


def plus(a: int, b: int) -> int:
    """
    Plus a with b.
    :param a: int
    :param b: int
    :return: a + b
    """
    assert isinstance(a, int), "a must be type of int."
    assert isinstance(b, int), "b must be type of int."
    return a + b
